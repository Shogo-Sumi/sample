al は大変危険な関数である。以下の関数 set42 は与えられた文字列の変数に 42 を代入する。

// このような関数は絶対に書いてはならない。
function set42(key) {
  eval(`${key} = 42;`);
}

// 例:
set42("hello");
console.log(hello); // 42
set42 に意図せぬ動作 (例: システムに負荷を与える、セキュリティの問題となる挙動を取る) を行わさせるにはどのような引数を与えればいいか答えなさい。 ただし実行時エラーが発生しない引数としなさい。

〇evalについて
https://developer.mozilla.org/ja/docs/Web/JavaScript/Reference/Global_Objects/eval

・説明抜粋
eval() 関数の引数は文字列です。 ソース文字列をスクリプト本体として評価します。つまり、文と式の両方が使用可能です。
eval() は呼び出し元の権限で渡されたコードを実行します。悪意のある第三者に影響を受ける可能性のある文字列で eval() を実行すると、そのウェブページや拡張機能の権限において、ユーザーのマシン上で悪意のあるコードを実行してしまう可能性があります。さらに重要なことに、サードパーティのコードが eval() が（直接的な eval であれば）呼び出されたスコープを見ることができるため、攻撃者がローカル変数を読み取ったり変更したりすることができてしまいます。

〇考察
・システムに負荷を与える。
例えば無限ループに陥るような文字列を実行する文字列を引き渡すことが考えられる。"whire(true)"といった文言を含む文字列をeval()関数へ引き渡した場合、処理は完了せず、マシンのリソースを喰いつくすのではと考える。

・セキュリティの問題
例えばデータベースへ接続するWebサーバー側でeval()が使用されている場合、"delete from (テーブル名)"といったテーブルの削除や、"select * from (テーブル名)といった,本来アクセス権限、削除権限がないSQL文が実行されてしまう可能性があるのではないかと考える。
